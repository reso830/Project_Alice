# Tasks: Hosted Password Management

**Feature**: `045-auth-password-reset` | **Spec**: [spec.md](spec.md) | **Plan**: [plan.md](plan.md)

Conventions: tasks are small, ordered, and specific. `[P]` marks parallelizable tasks (different files, no shared edits). Status legend: `[x]` done · `[ ]` pending · `[~]` skipped.
Commands: `npm run test:run`, `npm run lint`.

Phase dependency: 01 → 02 → 03 → 04 → 05 → 06 → 07

**Additive rule**: phases are ordered so `npm run test:run` passes after every phase, not just at the end. **Parallel opportunity**: Phase 03 (Forgot Password) has no dependency on Phase 01 at all (no password field involved) and Phase 02 (Change Password) depends only on Phase 01's `validatePassword` (T001), not its recovery-guard work (T002) — both could be staffed in parallel after T001 lands if worked by more than one agent/developer. Phase 04 is the one phase that genuinely needs all of Phase 01 (the recovery-status state machine).

**Constitution validation areas**: this feature does not touch application-record validation (status transitions, required-field enforcement, URL validation, date handling on job applications) — those required areas are N/A here; existing suites covering them must stay green (verified in Release Prep). The validation logic this feature *does* add — password-policy compliance — gets its own explicit test coverage (T001, T003).

**Visual-Fidelity Mode**: Mixed (see [plan.md](plan.md#visual-fidelity-mode)). Tier 1 (automated geometry harness) is **skipped this feature** by proportionality decision — no `npm run test:visual` harness exists yet and standing one up is out of proportion to 3 modals reusing existing overlay chrome. Tier 2 (screenshot-vs-prototype visual judgment) is **not** skipped — every visual task below still requires it, self-served by the implementing agent after an in-session image-view preflight.

**Prerequisite**: none — this feature does not depend on any other in-flight feature branch.

## Phase summary

| Phase | Focus | Tasks | Stories |
|---|---|---|---|
| 01 | Foundational — password policy + recovery-detection state machine | T001–T004 | — |
| 02 | Change Password (Settings) | T005–T010 | US-1 |
| 03 | Forgot Password (Welcome, request) | T011–T015 | US-2 |
| 04 | Reset Password + expired-link state (Welcome, via link) | T016–T022 | US-3, US-4 |
| 05 | Polish & Cross-Cutting — Local/Demo isolation, a11y, copy | T023–T026 | US-5, US-6 |
| 06 | Release Prep | T027–T035 | — |
| 07 | Browser Smoke Test | T036–T042 | US-1–US-6 |

---

## Phase 01: Foundational — Password Policy + Recovery-Detection State Machine

**Purpose**: Groundwork shared across the rest of the feature. No user-visible surface ships yet.

- [x] T001 [P] Consolidate password-policy validation into `src/utils/validate.js`
  - **Target**: [src/utils/validate.js](../../src/utils/validate.js) (new `validatePassword(value)` + `PASSWORD_MIN = 8` export); [src/pages/welcome/LoginForm.js](../../src/pages/welcome/LoginForm.js), [src/pages/welcome/SignupForm.js](../../src/pages/welcome/SignupForm.js) (remove the duplicated local `PASSWORD_MIN` constant and inline length check, import `validatePassword` instead)
  - **Expected behavior**: One canonical 8-character-minimum rule and one canonical error string, used by both existing forms with no behavior change (same threshold, same UX — this is a pure extraction, not a policy change). Reconcile the two slightly different error strings currently in play ("Password must be at least 8 characters." in `LoginForm.js`/`SignupForm.js`) into a single string that later tasks (T008, T013, T018) also use.
  - **Constraints**: no change to the 8-character threshold itself (spec Non-Goal: no composition/strength-meter rules beyond the existing floor); `src/utils/validate.js`'s existing exports (`validateRequired`, `validateEmail`, `validateUrl`, etc.) are untouched.
  - **Validation/test**: unit tests for `validatePassword` (boundary at exactly 8 chars, empty string, whitespace-only) in `tests/utils/validate.test.js`; existing `tests/pages/welcome/*`/`tests/components/welcome.test.js` assertions covering `LoginForm`/`SignupForm` password validation continue passing unmodified.
  - **Out of scope**: any new composition rule (uppercase/digit/symbol requirements) — explicitly not part of this feature.

- [x] T002 Recovery-URL detection + guarded auth-event state machine in `authStore`
  - **Target**: [src/data/authStore.js](../../src/data/authStore.js) `init()` and the `onAuthStateChange` callback
  - **Expected behavior**: On `init()`, before any `await`, synchronously check `location.hash`/`location.search` for Supabase's recovery marker (`type=recovery`) and arm a `pendingRecovery` guard if present. Register `supabase.auth.onAuthStateChange(...)` **before** awaiting `getSession()` (today's code does the reverse). While the guard is armed: a bare `SIGNED_IN` event does **not** resolve `authenticated` (it's held, see plan.md Architecture / research.md D1); a `PASSWORD_RECOVERY` event resolves `status: 'password-recovery'` and disarms the guard; if neither arrives within a short timeout (~5–10s), resolve `status: 'recovery-expired'` and disarm the guard. The guard arms at most once per page load. `state`'s type/shape gains the two new status literals; `getAuthState()`/`subscribe()` are unchanged otherwise.
  - **Constraints**: does not change behavior for the no-recovery-URL case (today's `local-mode`/`unauthenticated`/`authenticated`/`demo` paths must be unaffected — `tests/data/authStore.test.js`/`tests/data/authStore.demo.test.js`'s existing assertions must keep passing without modification to their expectations). Guard timeout value is a constant, easy to find/tune (research.md flags the exact duration as something to verify against real timing, not a hard-coded magic number scattered in logic).
  - **Validation/test**: see T003.
  - **Out of scope**: `main.js`'s `render()` branch for the new statuses (Phase 04, T016) — this task only makes the statuses reachable in `authStore`, it does not wire anything downstream yet.

- [x] T003 [P] `authStore` recovery-guard test suite
  - **Target**: [tests/data/authStore.test.js](../../tests/data/authStore.test.js) (extend)
  - **Expected behavior**: Cover (a) no recovery URL → existing behavior unaffected; (b) recovery URL + `SIGNED_IN` then `PASSWORD_RECOVERY` (in that order) → resolves `password-recovery`, never transiently observable as `authenticated` by a subscriber; (c) recovery URL + neither event arrives → resolves `recovery-expired` after the timeout; (d) recovery URL + `SIGNED_IN` only (no `PASSWORD_RECOVERY` ever) → resolves `recovery-expired` after the timeout, not stuck on `authenticated`.
  - **Constraints**: mock `supabase.auth.onAuthStateChange`/`getSession()` — no real network/Supabase project needed for this suite (T004 covers the one thing this can't verify).
  - **Validation/test**: `npm run test:run tests/data/authStore.test.js`.
  - **Out of scope**: `main.js` integration (Phase 04).

- [~] T004 Manual verification — real recovery-link event ordering
  - **Target**: a test Supabase project (not a repo file)
  - **Expected behavior**: Trigger a real password-recovery email, click the link against a local `npm run dev` pointed at that project, and confirm the event ordering (research.md D1) actually holds end-to-end (real email, real redirect URL, real browser) for the installed `@supabase/auth-js@2.105.4`. If it doesn't hold as expected, revisit T002's guard logic before Phase 04 builds on it.
  - **Constraints**: exploratory/manual — not automatable in this pass; do once, document the observed behavior (a one-line note in research.md is sufficient, not a new artifact).
  - **Validation/test**: N/A (manual). Blocks Phase 04 in spirit (Phase 04's correctness depends on this being right), though Phase 04 can be *coded* against the assumption in parallel and adjusted if T004 finds it wrong.
  - **Out of scope**: building any automated test around a real Supabase project (out of proportion — T003's mocked suite is the durable coverage).
  - **Partially resolved, still skipped for the live-network portion (2026-07-10)**: the original web-search-based hypothesis in research.md D1 (`SIGNED_IN` before `PASSWORD_RECOVERY`) was checked against the actual installed/pinned `@supabase/auth-js@2.105.4` source (`node_modules/@supabase/auth-js/dist/main/GoTrueClient.js`, `_initialize()`/`onAuthStateChange()`/`_emitInitialSession()`) rather than left as an assumption — see research.md D1 for the exact lines. The real sequence is `INITIAL_SESSION` (not `SIGNED_IN`) then `PASSWORD_RECOVERY`; T002's guard code was already correct regardless (it holds any non-`PASSWORD_RECOVERY` event by design, not a specific one), so no logic change was needed — only the comments and T003's test suite were corrected to test the real event name. **What's still unverified is the live, end-to-end path**: a real email send, real redirect URL round-trip, and real browser load — none of which the source reading covers, since no test Supabase project/credentials were available in this environment. Residual risk is now narrower than before (the deterministic event-ordering logic is confirmed from the exact shipped code, not secondhand reports) but not zero.
  - **Decision (2026-07-10, user)**: residual risk accepted as-is; do not block Phase 01 closure or later phases on obtaining live Supabase credentials. Re-verify for real before or during Phase 04, which is what actually exercises this path against a mounted UI — that phase should not proceed to merge without this having been run at least once against a real recovery link.

**Checkpoint**: `validatePassword` and the recovery-status state machine exist and are tested (T001–T003). T004 (live end-to-end verification) is accepted-open by user decision (2026-07-10) — not blocking, but still owed before Phase 04 merges. Phases 02–04 can now proceed (02/03 don't need T002; 04 needs T002 and must get T004 run for real before/during its own work, not skipped again).

**Verification run (2026-07-10)**: `npm run test:run tests/data/authStore.test.js tests/data/authStore.demo.test.js tests/utils/validate.test.js tests/components/welcome.test.js tests/pages/welcome/` — 138/138 passed. `npm run lint` — clean. Full `npm run test:run` initially showed 1895/1896 passed; the one failure (`tests/release-metadata.test.js`, "documents the Hosted Startup Performance release surfaces with resolvable links") was pre-existing and unrelated to Phase 01's actual task list — it checked that `HostedAlice_StartupLoader/design_handoff_startup_loader` still exists, a directory removed from git earlier in this branch's history (commit `3158ab5`, before Phase 01 work began).

**Ratified as accepted Phase 01 collateral (2026-07-10)**: Codex's Phase 01 review flagged this as CRITICAL — the tasks.md conventions header states "phases are ordered so `npm run test:run` passes after every phase," and a red tree is a red tree regardless of which task caused it. Fixed by removing the one stale path assertion from `tests/release-metadata.test.js` (`3158ab5` predates Phase 01; the test just never got updated for it). This is explicitly out-of-scope for T001–T004 and is recorded here, not attributed to any Phase 01 task ID, so it doesn't get mistaken for new scope in a later phase's diff. Full suite re-confirmed 1896/1896 passing after the fix; carried forward as already-fixed in every subsequent phase's baseline.

---

## Phase 02: Change Password (Settings)

**Goal**: A signed-in Hosted Mode user can change their password from Settings.
**Independent Test** (spec.md US-1): Signed in on a hosted deploy, open Settings → Account → "Change password". Submit with a correct current password and a compliant new password → success notification, overlay closes, sign-out/sign-in with the new password succeeds. Submit with an incorrect current password → inline error, overlay stays open.

- [x] T005 `PATCH /api/account/password` route + both repository adapters
  - **Target**: [server/routes/account.js](../../server/routes/account.js) (new `router.patch('/password', ...)`, `requireAuth`-gated, alongside the existing `DELETE /`); [server/repositories/supabase/account.js](../../server/repositories/supabase/account.js) (new `changePassword(body)`); [server/repositories/account.js](../../server/repositories/account.js) (SQLite — new `changePassword()` stub)
  - **Expected behavior**: Per contracts/api.md §1 — hosted: validate both fields present and `newPassword.length >= 8` (else `VALIDATION_ERROR`/400); re-verify `currentPassword` via a fresh anon client's `signInWithPassword` (else `INVALID_PASSWORD`/401, mirroring `deleteAccount`'s step 1 exactly); update via the lazily-imported service-role admin client's `admin.auth.admin.updateUserById(userId, {password: newPassword})`; return `{updated: true}`. Local: unconditionally throw `{code: 'NOT_SUPPORTED', status: 501}`. Route handler maps thrown `{code, status}` to a JSON error body, mirroring the existing `DELETE /` handler's error mapping.
  - **Constraints**: `currentPassword`/`newPassword` are never logged (matches the existing comment on `DELETE /api/account`'s handler). Reuses the already-established lazy-import pattern for the admin client (`server/repositories/supabase/adminClient.js`) — do not eagerly import it at module load.
  - **Validation/test**: see T006.
  - **Out of scope**: any change to the existing `DELETE /` route or `deleteAccount()` — this task adds alongside it, does not touch it.

- [x] T006 [P] Server test suite for `PATCH /api/account/password`
  - **Target**: [tests/server/account.test.js](../../tests/server/account.test.js) (route wiring + `requireAuth` gating), [tests/server/repositories/supabase/account.test.js](../../tests/server/repositories/supabase/account.test.js) (hosted adapter), [tests/server/repositories/account.test.js](../../tests/server/repositories/account.test.js) (local adapter stub)
  - **Expected behavior**: Cover all four error paths (missing fields, weak new password, wrong current password, local `NOT_SUPPORTED`) plus the success path, matching contracts/api.md §1's table exactly.
  - **Validation/test**: `npm run test:run tests/server/`.
  - **Out of scope**: client-side tests (T010).

- [x] T007 [P] Client API function — `changePassword`
  - **Target**: [src/services/api.js](../../src/services/api.js) (new `changePassword({currentPassword, newPassword})`, modeled directly on the existing `deleteAccount()`)
  - **Expected behavior**: `PATCH /api/account/password` with the given body; surfaces `err.code`/`err.message` from the response for the modal to branch on (`INVALID_PASSWORD` vs. other).
  - **Constraints**: same demo-mode short-circuit shape as `deleteAccount()` (`isDemo()` → reject with a clear `DEMO_UNAVAILABLE`-style error) is **not** needed here since T009 already prevents the control from rendering in demo mode — but keep the function itself safe to call (no crash) if it somehow is, for defense-in-depth consistency with `deleteAccount()`'s own guard.
  - **Validation/test**: unit test alongside `tests/services/api.test.js`'s existing `deleteAccount` coverage, if that file exists; otherwise colocate with T010.
  - **Out of scope**: server-side logic (T005).

- [~] T008 `PasswordChangeModal` component *(Visual — Tier 2 only, see plan.md Visual-Fidelity Mode)*
  - **Target**: new [src/components/PasswordChangeModal.js](../../src/components/PasswordChangeModal.js), modeled structurally on [src/components/DeleteAccountModal.js](../../src/components/DeleteAccountModal.js) (backdrop + `role="alertdialog"`/`aria-modal`/`aria-labelledby`, body-scroll lock, full Tab focus trap, ESC/backdrop-close disabled while loading, `INVALID_PASSWORD` → inline error + stay open, other errors → close + caller toasts)
  - **Match**: `Alice_Change_ForgotPwd.zip` → `design_handoff_password_reset_modal/password-change-form.jsx` (whole file — `PasswordChangeModal`/`PcfFields`/`PasswordChangeFormStyles`) and `README.md` §"Flow 2 — Change password (Settings)" + §"Design Tokens" (`.pcf-*` chrome values: max-width 400px, radius 16px, indigo accent `#4F46E5`, input height 44px/radius 9–10px, error `#E5484D`, success icon `#22C55E` on `rgba(34,197,94,.12)`).
  - **Breakpoints/checkpoints**: single fixed-width card (400px max), no distinct mobile layout declared in the handoff — verify no overflow/clip down to 375px viewport width (not a declared breakpoint, just a don't-break floor).
  - **Translation note**: no existing Settings-area modal to reuse for chrome (the only precedent, `DeleteAccountModal.js`, is a differently-shaped confirm/danger dialog, not a form modal) — lift the `.pcf-*` CSS custom properties/values wholesale into `src/styles/main.css` as a new, self-contained style block, and replicate the prototype's DOM shape (header/sigil, three-field form, Cancel/Submit actions, success state) rather than retrofitting `DeleteAccountModal`'s single-input shape.
  - **Provenance**: `.pcf-*` styles — `lifted from prototype CSS` (password-change-form.jsx's inline `<style>` block). Structural/behavioral shell (focus trap, ESC handling, `INVALID_PASSWORD` contract) — `recreated manually`, reason: the prototype has no equivalent server-integration contract to lift; `DeleteAccountModal.js`'s already-shipped, already-reviewed implementation of that contract is the correct source instead.
  - **Constraints**: three fields (current/new/confirm password), each with an independent show/hide eye toggle per the handoff; validation gated behind a `touched`/first-submit flag (no errors before the first submit attempt); submit disabled while loading; success state replaces the form in the same card (green check, "Password updated", Done button that closes the modal — no navigation, matching README §Flow 2's "Done closes the modal (no navigation; user is already logged in)").
  - **Done when**: Tier 2 frozen-state screenshots of the built modal (form state + success state) reviewed side-by-side against the prototype's rendered output, self-served by the implementing agent after an in-session image-view preflight. (Tier 1 skipped this feature.)
  - **Validation/test**: see T010.
  - **Out of scope**: the Settings entry-point wiring (T009).
  - **Partially resolved, Tier 2 skipped (2026-07-10)**: the component itself is built — `.pcf-*` CSS custom properties/values (colors, radii, spacing, shadows, animation timings) were transcribed directly from `password-change-form.jsx`'s inline `<style>` block into `src/styles/main.css`, and the DOM shape (header, three fields with independent peek toggles, Cancel/Update actions, in-place success card with the × button persisting across both phases) mirrors the prototype's structure — but the **Tier 2 screenshot-vs-prototype visual review itself was not performed**: this implementation environment has no browser/screenshot tooling (no Playwright, no way to render and capture the built modal or view an image), so there was no in-session image-view preflight to self-serve against. Behavioral correctness is covered by T010's test suite (17 tests — field validation, focus trap, ESC/backdrop/loading-state gating, INVALID_PASSWORD vs. other-error handling, ARIA), but that is not a substitute for the pixel/layout check this task's "Done when" bar calls for. Residual risk: possible visual drift from the prototype (spacing, exact color rendering, animation feel) that only a rendered-page review would catch. Needs a real Tier 2 pass — either the operator viewing the built modal directly, or a vision-capable reviewer given screenshots — before or during Phase 07's Browser Smoke Test, which is the first point in this plan where the built UI is actually exercised in a real browser.

- [x] T009 Wire the Change Password entry point into Settings
  - **Target**: [src/pages/Profile.js](../../src/pages/Profile.js) `renderAccountGroup()` (or an adjacent group in the same `SETTINGS` section)
  - **Expected behavior**: A "Change password" control appears in the Account area, gated identically to the existing Delete Account control — reuse `resolveAccountMode()` (`'hosted' | 'local' | 'demo'`): visible/enabled only for `'hosted'`; hidden (or disabled with explanatory copy matching the Delete Account demo-mode treatment) for `'demo'`; absent for `'local'` (Local Mode never reaches Settings' hosted-only controls). Opens `PasswordChangeModal` (T008) on click; on success, `Toast.show('Password updated.', 'success')` (or equivalent) and closes.
  - **Constraints**: does not restructure the existing `ACCOUNT` group's Delete Account control — additive only.
  - **Validation/test**: see T010.
  - **Out of scope**: `PasswordChangeModal`'s internals (T008).

- [x] T010 [P] `PasswordChangeModal` + Settings-gating test suite
  - **Target**: new [tests/components/PasswordChangeModal.test.js](../../tests/components/PasswordChangeModal.test.js) (modeled on `tests/components/DeleteAccountModal.test.js`); [tests/pages/Profile.account.test.js](../../tests/pages/Profile.account.test.js) (extend for the new entry point's three-way mode gating)
  - **Expected behavior**: Modal-level: field validation (required, 8-char min via `validatePassword`, confirm-match), `INVALID_PASSWORD` keeps the modal open with an inline error, other errors close it, success shows the Done state. Profile-level: control renders for `hosted`, hidden/disabled for `demo`, absent for `local`.
  - **Validation/test**: `npm run test:run tests/components/PasswordChangeModal.test.js tests/pages/Profile.account.test.js`.

**Checkpoint**: US-1 is functionally complete and tested (T005–T007, T009–T010). `npm run test:run` passes (1937/1937) and `npm run lint` is clean. T008's Tier 2 visual review is the one open item — see its skip note; owed before Phase 07's Browser Smoke Test signs off on this phase's UI.

**Verification run (2026-07-10)**: targeted — `npm run test:run tests/server/account.test.js tests/server/repositories/account.test.js tests/server/repositories/supabase/account.test.js tests/services/api.test.js tests/services/api.demo.test.js tests/components/PasswordChangeModal.test.js tests/pages/Profile.account.test.js` — 100/100 passed. Full suite — `npm run test:run` — 1937/1937 passed (144 files, up from 143 — new `PasswordChangeModal.test.js`). `npm run lint` — clean.

**Scope note (2026-07-10, Codex review)**: `tests/release-metadata.test.js` also appears modified in Phase 02's diff. That change is **not** Phase 02 scope — it's the Phase 01 collateral fix ratified in Phase 01's checkpoint above, still uncommitted (nothing in this feature has been committed since the initial spec/plan/tasks package), so it surfaces in every subsequent phase's diff until a commit boundary separates them. No T005–T010 task touches that file.

**Post-review fix (2026-07-10, Codex finding)**: `PasswordChangeModal.js`'s `showDone()` removed `head` (containing `#pcf-title`, the element `aria-labelledby="pcf-title"` on the dialog pointed to) without re-anchoring the label, leaving the success-state dialog with a dangling `aria-labelledby` reference. Fixed by giving the success card's own heading `id="pcf-title"` so the same attribute stays valid across both phases. New regression test added to `tests/components/PasswordChangeModal.test.js` asserting `#pcf-title` resolves to a real, correctly-labeled element after `showDone()`. Re-verified: targeted suite 101/101 passed (100 + 1 new), full suite 1938/1938, lint clean.

---

## Phase 03: Forgot Password (Welcome, request)

**Goal**: A signed-out Hosted Mode user can request a recovery email from the Welcome sign-in form.
**Independent Test** (spec.md US-2): "Forgot password?" → enter a registered email → generic "if an account exists…" confirmation. Repeat with an unregistered-but-valid-format email → identical confirmation copy.

- [x] T011 `AuthOverlay`/`WelcomePage` view-union extension for `forgot`/`forgot_sent`
  - **Target**: [src/pages/welcome/AuthOverlay.js](../../src/pages/welcome/AuthOverlay.js) (`VALID_VIEWS` gains `'forgot'`, `'forgot_sent'`; `paint()` gains render branches for both, following the exact precedent `verification_sent` already sets — reached via a form's `onSuccess`, not a click, chrome/footer hidden the same way); [src/pages/welcome/WelcomePage.js](../../src/pages/welcome/WelcomePage.js) (`VALID_AUTH_VIEWS` extended to match)
  - **Expected behavior**: `forgot` renders `ForgotPasswordForm` (T013) in the existing `form-slot`; `forgot_sent` renders inline confirmation content (no form), matching how `verification_sent` already renders inline content directly in `paint()` rather than mounting a separate component.
  - **Constraints**: no change to `login`/`signup`/`verification_sent`'s existing behavior.
  - **Validation/test**: see T015.
  - **Out of scope**: `reset-password`/`recovery-expired` (Phase 04).

- [x] T012 "Forgot password?" link on the sign-in form
  - **Target**: [src/pages/welcome/LoginForm.js](../../src/pages/welcome/LoginForm.js)
  - **Expected behavior**: A "Forgot password?" link/button appears below the password field, **login view only** (per README §"Screens/Views" — not shown on the signup form). Clicking it calls the overlay's `onSwitch('forgot')` (the same mechanism `AuthOverlay`'s footer swap-link already uses), not a page navigation.
  - **Constraints**: does not alter `LoginForm`'s existing validation/submit behavior.
  - **Validation/test**: see T015.

- [~] T013 `ForgotPasswordForm` component *(Visual — Tier 2 only)*
  - **Target**: new [src/pages/welcome/ForgotPasswordForm.js](../../src/pages/welcome/ForgotPasswordForm.js), mounted-into-slot pattern matching [src/pages/welcome/LoginForm.js](../../src/pages/welcome/LoginForm.js)/[SignupForm.js](../../src/pages/welcome/SignupForm.js) (`mountForgotPasswordForm(container, {onSuccess})` returning an unmount fn). Also touches [src/pages/welcome/WelcomePage.js](../../src/pages/welcome/WelcomePage.js)'s `handleVerificationCallback()` and exports a shared `RECOVERY_URL_MARKER` from [src/data/authStore.js](../../src/data/authStore.js) — both directly necessitated by resolving this task's redirect-URL question (see below), not scope creep.
  - **Match**: `design_handoff_password_reset_modal/wr-auth.jsx` — `ForgotPasswordModal`'s **form phase only** (lines defining the email field + submit button + "Back to sign in" link; the "sent" phase is T011's `forgot_sent` view, not part of this component) — and `README.md` §"Step 1a — Email entry".
  - **Breakpoints/checkpoints**: inherits `AuthOverlay`'s existing responsive behavior (this form mounts into the same `form-slot` `LoginForm`/`SignupForm` already use) — verify at the same widths `AuthOverlay` is already exercised at; no new breakpoint logic.
  - **Translation note**: this form reuses the **live** `AuthOverlay`/`.auth-overlay__*` chrome (header/sigil/close button/footer are `AuthOverlay`'s, not reproduced here) — per plan.md's Visual-Fidelity Mode translation note, the prototype's own `.scrim`/`.modal`/`.m-head` classes are its stand-in for chrome this codebase already has for real; only the **field-level** markup (email input, submit button, "Back to sign in" link) is new, styled with the existing `.auth-overlay__*`/`auth-form__*` token classes `LoginForm.js` already establishes, not a new class namespace.
  - **Provenance**: field layout/copy — `lifted from prototype CSS`/copy (email field styling matches `LoginForm.js`'s existing `.auth-form__field` pattern, which itself already matches the prototype's field styling — no new tokens needed). Copy strings ("Forgot your password?", "We'll email you a link to reset it.", "Send reset link" / "Sending…") — `lifted from prototype` (README §Step 1a) verbatim.
  - **Constraints**: one field (email), reusing `LoginForm.js`'s existing email-format regex/validation pattern (not a new one); submit calls `supabase.auth.resetPasswordForEmail(email, {redirectTo: emailRedirectUrl})` **for every syntactically-valid-format email**, regardless of whether Supabase reports the account exists (FR-8/AC-5 — contracts/api.md F1); `onSuccess` fires regardless of Supabase's actual result (non-enumeration — the UI must not branch on success vs. "user not found"); "Back to sign in" calls `onSwitch('login')`.
  - **Done when**: Tier 2 screenshot of the form state reviewed against the prototype, self-served.
  - **Validation/test**: see T015.
  - **Out of scope**: the `forgot_sent` confirmation view (T011); the redirect-URL env var question (research.md D4 — inspect during this task, escalate if `VITE_AUTH_EMAIL_REDIRECT_URL` turns out unsuitable rather than guessing).
  - **Redirect-URL question resolved, escalated as instructed (2026-07-10)**: inspected the actual configured value (`.env.local.bak`, `docs/deployment.md`) — `VITE_AUTH_EMAIL_REDIRECT_URL=http://localhost:5173/?auth=callback`, **not** a bare origin. Reusing it as-is for `resetPasswordForEmail`'s `redirectTo` would make a recovery link also carry `?auth=callback`, colliding with `WelcomePage.js`'s existing `handleVerificationCallback()` (which shows an "Email verified. You can sign in now." banner on that exact marker) — a real bug, not hypothetical. Escalated to the user per this task's own instruction rather than guessing; **decision: reuse the same URL (already allow-listed in Supabase's dashboard, zero new config), and fix the consuming side instead.** `handleVerificationCallback()` now also checks for Supabase's recovery marker (`type=recovery`, exported as `RECOVERY_URL_MARKER` from `authStore.js` so both checks share one definition) and skips the verification banner when present — `?auth=callback` is still stripped from the URL either way. No second env var introduced; `docs/deployment.md`/T032 unaffected (that task's "only if a second env var was needed" condition did not trigger).
  - **Tier 2 skipped (2026-07-10)**: same environment limitation as T008 (Phase 02) — no browser/screenshot tooling available (see 2026-07-10 conversation: user confirmed a browser/Playwright MCP server is the long-term fix, explicitly deferred, not set up now). Copy/layout were transcribed directly from `wr-auth.jsx`/README §Step 1a, and the component reuses `AuthOverlay`'s already-shipped, already-reviewed chrome (no new chrome to visually re-verify) plus `LoginForm.js`'s already-established field styling — residual visual risk is narrower than T008's (less new visual surface), but still unverified by an actual render. Owed before Phase 07's Browser Smoke Test.

- [x] T014 Wire the non-enumerating `forgot_sent` confirmation
  - **Target**: [src/pages/welcome/AuthOverlay.js](../../src/pages/welcome/AuthOverlay.js) (`forgot_sent` render branch, part of T011 — split out here only if T011's branch needs the submitted email address threaded through for the confirmation copy)
  - **Expected behavior**: Confirmation copy: *"If an account exists for **{email}**, we've sent a password reset link. Click it to choose a new password."* (README §Step 1b, verbatim) — identical regardless of whether the email was actually registered. "Back to sign in" returns to `login`.
  - **Constraints**: no "open the link" demo/stand-in affordance (the design handoff's "Open reset link (demo)" button is explicitly a prototype-only stand-in per its own code comment — production reaches Reset Password only via a real emailed link, Phase 04).
  - **Validation/test**: see T015.

- [x] T015 [P] Forgot Password test suite
  - **Target**: new [tests/pages/welcome/ForgotPasswordForm.test.js](../../tests/pages/welcome/ForgotPasswordForm.test.js); extend [tests/components/welcome.test.js](../../tests/components/welcome.test.js) or the equivalent `AuthOverlay`/`WelcomePage` test file for the `forgot`/`forgot_sent` view transitions and `LoginForm`'s new link
  - **Expected behavior**: malformed email → inline error, `resetPasswordForEmail` never called; valid-format email (mock both a "success" and an "error" provider response) → `resetPasswordForEmail` called both times, identical `forgot_sent` copy both times; "Forgot password?" link absent on the signup view.
  - **Validation/test**: `npm run test:run tests/pages/welcome/ tests/components/welcome.test.js`.
  - **Also updated (2026-07-10)**: `tests/components/welcome.test.js` had an existing test literally titled "does not render a Forgot-password link in signin mode (spec: no custom in-app reset UI)" — a direct encoding of the feature-018 decision this feature reverses. Inverted to assert the link now renders, per spec.md's Problem Statement. The sibling "...in signup mode" test needed no change (still correct — the link stays login-only). Also added a `?auth=callback` + recovery-marker collision test (see T013's redirect-URL note) and a `resetPasswordForEmail` mock to the file's hoisted Supabase mock (previously only `signInWithPassword`/`signUp`).

**Checkpoint**: US-2 is functionally complete and tested (T011, T012, T014, T015). `npm run test:run` passes (1964/1964) and `npm run lint` is clean. T013's Tier 2 visual review is the one open item — see its skip note; owed before Phase 07's Browser Smoke Test, same as T008 (Phase 02).

**Verification run (2026-07-10)**: targeted — `npm run test:run tests/pages/welcome/ForgotPasswordForm.test.js tests/components/welcome.test.js` — 98/98 passed. Broader — `tests/pages/welcome/ tests/components/ tests/utils/ tests/data/` — 904/904 passed. Full suite — `npm run test:run` — 1964/1964 passed (145 files, up from 144 — new `ForgotPasswordForm.test.js`). `npm run lint` — clean.

---

## Phase 04: Reset Password + Expired-Link State (Welcome, via recovery link)

**Goal**: A user who opens a valid recovery link sets a new password and lands back on a logged-out Welcome/login screen; a user who opens an expired/invalid link sees a clear explanation instead of a confusing form.
**Independent Test** (spec.md US-3/US-4): open a valid recovery link → Reset Password overlay (not login) → submit → success → recovery session ends → standard logged-out Welcome. Open an expired/already-used link → dedicated expired-link state on load.
**Depends on**: Phase 01 (T002's recovery-status state machine) — do not start before T002/T004 land.

- [x] T016 `main.js`/`WelcomePage` initial-view threading for the recovery statuses
  - **Target**: [src/main.js](../../src/main.js) `render()` (new branch: `state.status === 'password-recovery' | 'recovery-expired'` → `mountWelcome({initialAuthView: state.status === 'password-recovery' ? 'reset-password' : 'recovery-expired'})`); `mountWelcome()` (gains an optional parameter, threaded into `WelcomePage.mount()`); [src/pages/welcome/WelcomePage.js](../../src/pages/welcome/WelcomePage.js) `mount()` (accepts and threads an initial view into `_deps.openAuthOverlay`/the overlay render call, instead of always starting `null`)
  - **Expected behavior**: On boot with a recovery-shaped session, the Welcome shell mounts (same starfield/hero/footer as always) with the overlay **already open** in the `reset-password` or `recovery-expired` view — no click required, and the standard login form is never shown first.
  - **Constraints**: `mountWelcome()`'s existing no-argument call sites (`unauthenticated` routing) are unaffected — the new parameter is optional and defaults to the current `null`/login behavior. Respect `mountWelcome()`'s existing `if (_welcomeMounted) return;` idempotency guard — the recovery statuses are only reachable at initial boot (per data-model.md §1's invariant), so this should not collide with an already-mounted Welcome in practice; verify this holds rather than assuming.
  - **Validation/test**: see T022.
  - **Out of scope**: any change to `mountAppShell()`/`mountConfigError()` or their branch conditions.
  - **Idempotency-guard interaction found and resolved (2026-07-10)**: verified (not assumed) that `mountWelcome()`'s existing `if (_welcomeMounted) return;` guard *does* collide with one real transition this feature introduces — not at initial boot (that part of the constraint holds exactly as stated), but afterward: when `reset-password`/`recovery-expired`'s own `signOut()` call (T020 success path or T021 abandon path) flips `authStore` to `unauthenticated`, `render()`'s plain `mountWelcome()` call would silently no-op (Welcome is already mounted) and strand the stale overlay view on screen. Fixed by adding a small branch to `render()`'s `unauthenticated` case, *not* by touching `mountWelcome()`'s guard itself: when Welcome is already mounted and `WelcomePage.getAuthView()` is `reset-password`/`recovery-expired`, it calls the new `WelcomePage.setAuthView('login')` (repaints the already-mounted overlay in place) and surfaces any staged notice via a small extracted `surfacePendingNotice()` helper (shared with `mountWelcome()`'s existing notice-surfacing code) — no remount, no DOM teardown/rebuild. `WelcomePage.getAuthView()`/`setAuthView()` already existed (Phase 06 state machine) and needed no changes.

- [x] T017 `AuthOverlay`/`WelcomePage` view-union extension for `reset-password`/`recovery-expired`
  - **Target**: [src/pages/welcome/AuthOverlay.js](../../src/pages/welcome/AuthOverlay.js) (`VALID_VIEWS` gains both; render branches added); [src/pages/welcome/WelcomePage.js](../../src/pages/welcome/WelcomePage.js) (`VALID_AUTH_VIEWS` extended to match)
  - **Expected behavior**: `reset-password` renders `ResetPasswordForm` (T018); `recovery-expired` renders inline expired-link content (T019) — both reachable **only** via T016's initial-view threading (never via a click, unlike every other view), and `recovery-expired` additionally reachable if `ResetPasswordForm`'s submit fails citing an invalid/expired session (T020).
  - **Constraints**: neither view exposes a mode-swap link to `signup` (a recovery flow has nothing to do with creating a new account) — only a path back to `login`/`forgot` as applicable. The `reset-password` view's close (×), Escape, and backdrop-click handling **cannot fall through to `AuthOverlay`'s generic `close()`/`setAuthView(null)` unmodified** the way every other view does — abandoning an active recovery session must also end it (T021 owns the actual `signOut()` wiring; this task's job is to make sure `reset-password`'s close path is distinguishable from the generic one, not to bypass it by omission).
  - **Validation/test**: see T022.

- [~] T018 `ResetPasswordForm` component *(Visual — Tier 2 only)*
  - **Target**: new [src/pages/welcome/ResetPasswordForm.js](../../src/pages/welcome/ResetPasswordForm.js), mounted-into-slot pattern matching `LoginForm.js`
  - **Match**: `design_handoff_password_reset_modal/wr-auth.jsx` — `NewPasswordModal`'s **form phase** (new-password + confirm-new-password fields, submit button, "Back to sign in" link — the "done" success phase is handled by T020's success path via `setAuthNotice`/Toast after reroute, not rendered in-place here per the spec Clarification that a successful reset signs the user out rather than showing an in-overlay success card) — and `README.md` §"Step 2 — Set a new password".
  - **Breakpoints/checkpoints**: inherits `AuthOverlay`'s existing responsive behavior, same as T013.
  - **Translation note**: reuses the live `AuthOverlay` chrome, same as T013 — only the two-field form body is new, using the existing `.auth-form__field`/eye-toggle pattern `LoginForm.js` already establishes (each field with its own independent show/hide toggle per the handoff).
  - **Provenance**: field layout — `lifted from prototype CSS` (matches `LoginForm.js`'s existing field pattern, which already reflects the prototype's field styling). Copy ("Set a new password", "Choose a new password for your account.", "Update password" / "Updating…", validation messages) — `lifted from prototype` (README §Step 2) verbatim, reconciled against T001's canonical `validatePassword` error string where the two differ (research.md's noted copy-reconciliation item).
  - **Constraints**: two fields only (new password, confirm — no current-password field, since the whole point of a recovery flow is the user doesn't have one to prove); validation gated behind `touched`/first-submit; reuses `validatePassword` (T001) for the length check.
  - **Done when**: Tier 2 screenshot of the form state reviewed against the prototype, self-served.
  - **Validation/test**: see T022.
  - **Out of scope**: the submit success/failure flow (T020); the abandon-path sign-out behind the "Back to sign in" link (T021 — this task renders the link and fires `onSwitch`/`onClose`, T021 wires what those calls actually do for this specific view); the `recovery-expired` view (T019).
  - **Tier 2 skipped (2026-07-10)**: same environment limitation as T008 (Phase 02) and T013 (Phase 03) — no browser/screenshot tooling available (long-term fix — a browser/Playwright MCP server — remains explicitly deferred by the user, not set up this phase). The two-field form (each with its own independent show/hide toggle) reuses `LoginForm.js`'s already-established `.auth-form__field`/eye-toggle pattern and mounts into `AuthOverlay`'s already-shipped, already-reviewed chrome — no new chrome to visually re-verify — but the copy/spacing transcribed from `NewPasswordModal`'s form phase was never actually rendered and compared against the prototype. Behavioral correctness (validation, submit success/failure/expired-session split, the T021 in-flight close-gate) is covered by 19 tests in `tests/pages/welcome/ResetPasswordForm.test.js` plus integration coverage in `tests/components/welcome.test.js`, which is not a substitute for this task's "Done when" pixel/layout check. Owed before Phase 07's Browser Smoke Test.

- [~] T019 `recovery-expired` view content *(Visual — Tier 2 only, small)*
  - **Target**: [src/pages/welcome/AuthOverlay.js](../../src/pages/welcome/AuthOverlay.js) (`recovery-expired` render branch, part of T017 — split out here for the visual-fidelity task pattern since it has no prototype screen of its own to match)
  - **Match**: **no direct prototype screen** — the design handoff does not model an expired-link state (it only ever "clicks the reset link" successfully in its demo stand-in). This task's copy/layout is derived from the existing inline-message pattern `AuthOverlay`'s `verification_sent` view already establishes (icon/title/message/action layout), not a new visual language.
  - **Breakpoints/checkpoints**: inherits `AuthOverlay`'s existing responsive behavior, same as `verification_sent`.
  - **Translation note**: N/A (no cross-stack source to translate — this is new content within existing chrome).
  - **Provenance**: `recreated manually` — reason: no prototype coverage exists for this state (spec Clarification, 2026-07-10, resolved this gap independent of the design handoff).
  - **Constraints**: message: "This reset link has expired — request a new one." (or equivalent, matching the spec Clarification's wording); one action back to `forgot` (not `login` directly, so the user isn't dead-ended without a way to request a fresh link).
  - **Done when**: Tier 2 screenshot reviewed for basic visual consistency with the rest of `AuthOverlay`'s inline-message views (no prototype to compare against, so this is an internal-consistency check, not a prototype-match check).
  - **Validation/test**: see T022.
  - **Tier 2 skipped (2026-07-10)**: same environment limitation as T018 above (no browser/screenshot tooling). Deliberately kept simpler than `forgot_sent`'s mail-icon treatment — no icon, matching `verification_sent`'s icon-less inline-message style — specifically to minimize new visual surface pending a real review. Owed before Phase 07's Browser Smoke Test.

- [x] T020 Wire `updateUser` success (explicit sign-out) and failure (→ expired) paths
  - **Target**: [src/pages/welcome/ResetPasswordForm.js](../../src/pages/welcome/ResetPasswordForm.js) (submit handler); [src/data/authStore.js](../../src/data/authStore.js) (reuse existing `signOut()`/`setAuthNotice()`, no new exports needed)
  - **Expected behavior**: Submit calls `supabase.auth.updateUser({password: newPassword})`. On success: `authStore.setAuthNotice('Password updated. Sign in with your new password.', 'success')` → `authStore.signOut()` (per spec Clarification 2026-07-10 — explicit, not passive) → the resulting `unauthenticated` state routes to standard Welcome/login via the existing `render()` path, and `mountWelcome()`'s existing `consumeAuthNotice()`/Toast mechanism (already used for the delete-account reroute) surfaces the notice post-reroute. On failure citing an invalid/expired session (FR-11's runtime re-check — the recovery session lapsed between load and submit): `setView('recovery-expired')` (T017/T019's view) rather than a generic inline error. On any other failure (network, etc.): inline error, form stays usable for retry (FR-13 — no provider internals exposed).
  - **Constraints**: does not skip `setAuthNotice()` — a bare `Toast.show()` called before `signOut()`'s reroute would be lost when `document.body` clears, per the same reasoning `mountWelcome()`'s comment already documents for the delete-account case.
  - **Validation/test**: see T022.
  - **Note (revised 2026-07-10, post-review)**: an earlier version of this task deliberately never reset the pending/loading flag back to idle on the success path (reasoning: the form is about to be torn down by the reroute anyway). Code review (Codex) correctly flagged this as a real dead-end: if `signOut()` rejects, the reroute never happens, and the form — plus `AuthOverlay`'s close gate, which keys off this same flag — is stuck disabled forever with no way out, even though the password update itself already succeeded. Fixed: `setPending(false)` now runs in a `finally` around the `signOut()` attempt, regardless of success or failure. On the happy path this is a harmless no-op (the reroute has typically already torn the form down by the time it runs); on a failed `signOut()`, it's what re-enables the overlay's close affordances so the user isn't trapped — see T021's revised note for the other half of this fix.

- [x] T021 Wire explicit sign-out on the Reset Password **abandon** path (close / Escape / backdrop / "Back to sign in")
  - **Target**: [src/pages/welcome/AuthOverlay.js](../../src/pages/welcome/AuthOverlay.js) (the × close button, `onKeydown`'s Escape handling, and the backdrop click handler — all currently generic across every view); [src/pages/welcome/ResetPasswordForm.js](../../src/pages/welcome/ResetPasswordForm.js) ("Back to sign in" link's click handler)
  - **Expected behavior**: Per spec Clarification (2026-07-10) / research.md D5 / contracts/api.md R7 — while `AuthOverlay`'s current view is `reset-password` **and no submit is in flight**, closing via the × button, Escape, a backdrop click, or the form's "Back to sign in" link calls `authStore.signOut()` (no `setAuthNotice()` — nothing succeeded, so no notification) before the overlay's normal close/reroute behavior proceeds. Every other view's close behavior is completely unchanged — this is additive, scoped to `reset-password` only, not a change to `close()`'s general contract.
  - **Constraints**: while a submit is in flight (`loading` state, T020), close/Escape/backdrop are already disabled per the existing `DeleteAccountModal.js`-style convention (spec Edge Case: "User closes/ESC/backdrop-clicks an in-progress request") — this task's sign-out path only fires when idle, never racing T020's own `signOut()` call on a concurrent submit. Do not duplicate `signOut()` calls if the user completes a submit and the overlay's teardown also runs some shared close routine — trace the actual call path before wiring both, rather than assuming they're independent.
  - **Validation/test**: see T022.
  - **Out of scope**: `recovery-expired`'s close behavior (that view never held a confirmed recovery session — see data-model.md §1 — so it has nothing to sign out of; its close is the ordinary generic path, matching `verification_sent`).
  - **In-flight gate corrected during implementation (2026-07-10)**: an earlier pass through this task had skipped the "no submit in flight" constraint entirely (mistakenly treating it as having no precedent in this file), which would have let an abandon action race a concurrent `updateUser` submit. Caught by re-reading this task's own constraint text and `DeleteAccountModal.js`'s actual `loading`/`cancelBtn.disabled`/Escape/backdrop gating before calling the task done. Fixed: `ResetPasswordForm.js`'s `setPending()` now reports pending state via a new `onPendingChange` callback; `AuthOverlay.js` tracks it in a `resetPending` closure variable (reset at the top of every `paint()`), gates `close()` on it when `state.view === 'reset-password'` (so ×/Escape/backdrop/"Back to sign in" all share the one no-op path), and visually disables the × button and the "Back to sign in" link while pending, mirroring `DeleteAccountModal`'s `cancelBtn.disabled = loading`.
  - **Abandon-path race corrected post-review (2026-07-10)**: code review (Codex) flagged that `close()`'s original fire-and-forget `signOut()` call teardown the overlay in the same tick, without confirming the sign-out attempt actually succeeded — so a rejected `signOut()` would hide the only UI representing an active recovery session while that session was never actually ended, violating this task's own "must also end it" contract. Fixed: for `reset-password`, `close()` now reuses `resetPending` itself while its *own* `signOut()` attempt is outstanding (blocking re-entrant close calls the same way T021's in-flight gate does, and disabling the × button) and only finishes tearing the overlay down — via a new internal `finishClose()` — once that attempt settles, success or failure. A rejected `signOut()` still finishes the close rather than leaving the user stuck (the alternative — trapping them in an un-closeable modal over a transient network error — is worse than a possibly-stale recovery session, which a subsequent sign-in attempt overwrites regardless). `recovery-expired` and every other view are unaffected — they still close synchronously, immediately, exactly as before.

- [x] T022 [P] Reset Password + expired-link test suite
  - **Target**: new [tests/pages/welcome/ResetPasswordForm.test.js](../../tests/pages/welcome/ResetPasswordForm.test.js); extended [tests/main.test.js](../../tests/main.test.js) for T016's `render()` branch; extended `tests/components/welcome.test.js`'s `AuthOverlay` tests for T017/T019's new views
  - **Expected behavior**: `password-recovery` status → Welcome mounts with `reset-password` open (not login); `recovery-expired` status → Welcome mounts with the expired-link view open; successful `updateUser` → `signOut()` called, notice staged, reroute occurs (and pending is released even if that `signOut()` rejects — no stuck form); `updateUser` failing with an invalid-session error → transitions to `recovery-expired` in place (no reroute); mismatch/below-policy new password → inline error, no `updateUser` call. **Abandon-path coverage (T021)**: closing via ×, Escape, and backdrop click while idle on `reset-password` → `signOut()` called and awaited before the overlay tears down, no notice staged, reroute to standard Welcome/login; same for the "Back to sign in" link; a rejected abandon-path `signOut()` still finishes closing (not stuck); a second close attempt while the first is still settling is a no-op (no duplicate `signOut()` calls); closing while a submit is in flight → close is a no-op (matches the existing loading-disables-close convention) and does **not** additionally call `signOut()`; closing `recovery-expired` (contrast case) → ordinary *synchronous* close, no `signOut()` call (nothing to end).
  - **Validation/test**: `npm run test:run tests/pages/welcome/ tests/main.test.js tests/components/welcome.test.js` — all green. Added: 17 tests in new `ResetPasswordForm.test.js` (rendering, per-field toggles, touched-gated validation, `updateUser` success/failure/expired-session split, `onPendingChange`, in-flight double-submit guard, "Back to sign in", unmount) + 3 more post-review (T021 `onPendingChange` sequencing, `backLink.disabled`, pending released when a post-success `signOut()` rejects) = 20 total; 5 new tests in `main.test.js` (`password-recovery`/`recovery-expired` initial-view threading, the ended-session return-to-login reroute with/without a notice, the no-op case when Welcome already shows an unrelated view); 20 new tests in `welcome.test.js`'s new "AuthOverlay — Reset Password + expired-link state" describe block (view chrome/footer for both views, submit success calling `signOut`, expired-session transition, non-expired inline error, "Request a new link" → `forgot` not `login`, abandon-while-idle via ×/back-link/backdrop/Escape awaiting `signOut()` before closing, a rejected abandon-path `signOut()` still closing, a second concurrent close attempt not duplicating `signOut()`, abandon-while-idle on `recovery-expired` NOT signing out and closing synchronously, the T021 in-flight no-op via ×/back-link/backdrop/Escape, the × button's disabled/re-enabled visual state, and a rejected post-success `signOut()` re-enabling × instead of leaving the user stuck).

**Checkpoint**: US-3 and US-4 are independently functional and testable. `npm run test:run` passes (2009/2009) and `npm run lint` is clean. Two MAJOR findings from code review (Codex) — a permanently-stuck overlay if the post-success `signOut()` rejects, and the abandon path tearing down the overlay before its own `signOut()` attempt was confirmed — were caught and fixed (see T020/T021's revised notes) before this checkpoint closed. T018/T019's Tier 2 visual review is the one remaining open item — see their skip notes; owed before Phase 07's Browser Smoke Test, same as T008 (Phase 02) and T013 (Phase 03).

---

## Phase 05: Polish & Cross-Cutting

**Purpose**: Close out the two isolation acceptance criteria that aren't tied to a single build phase, plus a consistency pass across all three new surfaces.

- [x] T023 [P] Local Mode isolation verification
  - **Target**: new or extended test asserting `authStore` status `'local-mode'` never reaches `mountWelcome()` (structural — already true today per `render()`'s existing branch table; this task adds explicit test coverage for the *new* surfaces specifically, not just the pre-existing shell behavior) — likely `tests/main.test.js`
  - **Expected behavior**: confirms no code path in this feature accidentally makes Forgot Password / Change Password reachable in Local Mode (e.g., a future edit to `resolveAccountMode()` or `render()` regressing this would be caught here).
  - **Validation/test**: `npm run test:run tests/main.test.js`.
  - **Done (2026-07-10)**: added a new `bootstrap — Local Mode isolation (feature 045, T023)` describe block asserting `WelcomePage.mount` is never called and `#welcome-root` never appears for `state.status === 'local-mode'` — the structural guarantee `render()`'s branch table already provided (only `unauthenticated`/`password-recovery`/`recovery-expired` call `mountWelcome()`), now with an explicit regression test scoped to this feature's own new surfaces rather than relying on the pre-existing shell-mounting tests to imply it.

- [x] T024 [P] Demo Mode isolation — consolidated check
  - **Target**: [tests/pages/Profile.account.test.js](../../tests/pages/Profile.account.test.js) (verify T010 already covers this; add the explicit demo-specific assertion if it doesn't)
  - **Expected behavior**: confirms Change Password's control is hidden/disabled with explanatory copy in `demo` mode, matching the Delete Account precedent exactly (same copy pattern, not just "absent").
  - **Validation/test**: `npm run test:run tests/pages/Profile.account.test.js`.
  - **Done (2026-07-10)**: verified at the implementation level (`Profile.js` `renderAccountGroup()`) that Change Password already shares the *exact same* early-return demo-mode block as Delete Account — not a separate/parallel copy, the literal same "Account management isn't available in the demo." note, since `renderPasswordRow()` is only ever appended in the `'hosted'` branch and the `'demo'` branch returns before reaching it. The gap was test-coverage only: T010's existing demo test for Change Password asserted the button was absent but never asserted the explanatory copy was present (unlike the sibling Delete Account demo test, which does). Extended the test to assert `settings.textContent` contains the shared copy, closing that gap.

- [x] T025 Accessibility pass across the three new overlays
  - **Target**: [src/components/PasswordChangeModal.js](../../src/components/PasswordChangeModal.js), [src/pages/welcome/ForgotPasswordForm.js](../../src/pages/welcome/ForgotPasswordForm.js), [src/pages/welcome/ResetPasswordForm.js](../../src/pages/welcome/ResetPasswordForm.js)
  - **Expected behavior**: verify (not re-implement — these inherit focus trap/ESC/ARIA from `AuthOverlay`/`DeleteAccountModal`'s existing patterns) that each overlay has a working focus trap, ESC-to-close (except mid-submit, matching `DeleteAccountModal`'s loading-state guard), `role="dialog"`/`aria-modal`/labelled-by, and that error/status regions carry `aria-live`. Confirm no gap was introduced by the new field-level markup (e.g., a missing `aria-label` on a new eye-toggle button).
  - **Validation/test**: manual keyboard-only walkthrough of all three overlays; no new automated test expected beyond what T010/T015/T022 already assert for ARIA attributes.
  - **Done (2026-07-10) — two real gaps found and fixed**: `ForgotPasswordForm.js`/`ResetPasswordForm.js` (both mount into `AuthOverlay`'s shared dialog/focus-trap/ESC chrome, already verified) had no gaps — their field errors, error regions, and status spans all already carry `aria-live="polite"`, matching `LoginForm.js`/`SignupForm.js`'s pre-existing convention. `PasswordChangeModal.js` (the one standalone modal, not reusing `AuthOverlay` chrome) had two: (1) its three `.pcf-err-msg` field-error regions had no `aria-live` at all — a real deviation from the `aria-live`-on-every-field-error convention every other form in this feature (and `LoginForm`/`SignupForm` before it) follows; fixed by adding `aria-live="polite"`. (2) its `.pcf-peek` show/hide toggles were excluded from the tab order (`tabIndex = -1`) — unique to this component; no other password-visibility toggle in the codebase (`LoginForm`, `SignupForm`, `ResetPasswordForm`) does this, meaning a keyboard-only user could not reach "Show password" in the Change Password modal at all; fixed by removing the `tabIndex` override. Added one regression test (`tests/components/PasswordChangeModal.test.js`) asserting both. Dialog semantics (`role="dialog"`/`aria-modal`/`aria-labelledby`, correctly re-anchored on the success-card swap — see the Phase 02 review fix), the focus trap, and ESC/backdrop-disabled-while-loading were already correct and already covered by T010's existing tests; a Tab-key focus-trap-wrap test is not present for either `PasswordChangeModal` or its own precedent `DeleteAccountModal.test.js` — a pre-existing, codebase-wide gap predating this feature, out of scope per this task's "no gap introduced by the new field-level markup" framing (not a regression from Phases 01–04).

- [x] T026 `npm run lint` clean
  - **Target**: all files touched by Phases 01–04
  - **Validation/test**: `npm run lint` exits clean.
  - **Done (2026-07-10)**: `npm run lint` exits clean (`eslint src tests server shared`, no output).

**Checkpoint**: All six user stories are independently functional and testable; cross-cutting isolation is explicitly verified, not just implied. `npm run test:run` passes (2011/2011) and `npm run lint` is clean.

---

## Phase 06: Release Prep

**Purpose**: Land documentation, version metadata, and operator-facing references in the same state the operator will merge. Runs before the Browser Smoke Test so the smoke test walks the to-be-merged state.

- [x] T027 Version bump — `package.json` (SemVer minor: new user-facing feature, no breaking change) and any in-app version display (`src/pages/welcome/shared/appMeta.js`'s `APP_VERSION` or equivalent); update matching version-string test assertions if they pin the literal.
  - **Done (2026-07-10)**: `package.json` `1.13.1` → `1.14.0`. `APP_VERSION` needed no direct edit — since v1.13.0 (#132) it's derived from `package.json` at build/test time via Vite's `__APP_VERSION__` define, so the bump propagates automatically. Updated three files that pin the literal in test assertions: `tests/release-metadata.test.js` (the main sync test + its title), `tests/pages/Profile.account.test.js` and `tests/pages/profile.aiSettings.test.js` (both assert `.update-settings__version-chip`'s rendered text, which reads the real `APP_VERSION`).
- [x] T028 Sync `package-lock.json` root fields (`version` + `packages[""].version`) to match — leave dependency versions untouched.
  - **Done (2026-07-10)**: both root `version` fields (line 3 top-level, line 9 inside `packages[""]`) updated to `1.14.0`. Verified via targeted grep that no dependency-pin `"version"` fields (e.g. `underscore`'s `^1.13.1` range pins) were touched.
- [x] T029 `docs/feature_roadmap.md` — tick `045-auth-password-reset` `[x]`, note the shipped version.
  - **Done (2026-07-10)**: `- [ ] 045-auth-password-reset` → `- [x] 045-auth-password-reset  ·  shipped v1.14.0`, matching the exact format of every prior entry in the same list.
- [x] T030 `CHANGELOG.md` — new `## [<new-version>] — <merge-date>` section (Keep-a-Changelog format, **Added**/**Changed** as applicable: Change Password, Forgot Password, Reset Password); update the `[Unreleased]`/`[<new-version>]` diff links at the bottom.
  - **Done (2026-07-10)**: added `## [1.14.0] — 2026-07-10` under `[Unreleased]` with a one-line summary and an **Added** section (Change Password, Forgot Password, Reset Password, `validatePassword`), each bullet tagged `(045-auth-password-reset)` — the same feature-id-tag convention feature 044's own entry used (recent merged entries use `(#PR-number)` tags instead, but that number doesn't exist yet for an unmerged feature; 044's entry, written at the same pre-merge point in its own Release Prep, sets the precedent for tagging by feature id instead of guessing a PR number). No **Changed** section — nothing existing changed behavior for a user; `validatePassword`'s extraction is additive/internal. Diff-links block: `[Unreleased]` now points `v1.14.0...HEAD`, new `[1.14.0]: .../compare/v1.13.1...v1.14.0` line added above the existing `[1.13.1]` line.
- [x] T031 `README.md` — add Features bullet(s) for the three new user-facing surfaces; update the `Current version` line. No per-feature `specs/045-…/` link under Further Reading (indexed via `docs/REPO_MAP.md` instead, per existing convention).
  - **Done (2026-07-10)**: one new bullet — "**Hosted password management**" — added after the existing "Delete Profile & User Data" bullet (same account-management grouping), covering all three surfaces in one line per the Features list's existing density convention (most bullets cover a whole feature area, not one sub-flow each). `Current version` line → `1.14.0`.
- [x] T032 `docs/deployment.md` — **only if** T013/research.md D4 concluded a second env var was needed for `resetPasswordForEmail`'s `redirectTo`; otherwise skip (existing `VITE_AUTH_EMAIL_REDIRECT_URL` reused, no runtime/env change).
  - **Skipped (2026-07-10), as this task's own condition anticipated**: T013 resolved D4 by reusing the existing `VITE_AUTH_EMAIL_REDIRECT_URL` — no second env var, no new runtime mode, no schema migration. `docs/deployment.md` is unchanged. (Not every feature gets a `docs/deployment.md` entry even when it introduces no env-var change — 043-legal-and-footer has none either; 042/044 added short "no new env vars" notes because they touched boot/runtime detection specifically, which 045 does not.)
- [x] T033 `docs/REPO_MAP.md` — add entries for the three new files (`PasswordChangeModal.js`, `ForgotPasswordForm.js`, `ResetPasswordForm.js`) and the new `PATCH /api/account/password` route; add a Spec Packages row for `specs/045-auth-password-reset/`.
  - **Done (2026-07-10)**: added rows for `PasswordChangeModal.js` (Components table), `ForgotPasswordForm.js`/`ResetPasswordForm.js` (Pages/Screens table, alongside the existing `AuthOverlay.js`/`LoginForm.js`/`SignupForm.js` rows), and a Spec Packages row for `specs/045-auth-password-reset/`. Also extended (not just added) the existing `AuthOverlay.js`, `LoginForm.js`, `server/routes/account.js`, `server/repositories/supabase/account.js`, and `server/repositories/account.js` rows with a "Feature 045: ..." clause each, following this file's established per-row cumulative-update convention (e.g. `Modal.js`'s row already carries "Feature 039: ...").
- [x] T034 Docs sanity check — grep the previous version string across `package.json`, `package-lock.json` (root fields only), `src/`, `README.md`, `CHANGELOG.md`, `docs/`; confirm only historical matches remain; confirm every new cross-link path exists; confirm the running app renders the new version.
  - **Done (2026-07-10)**: grepped `1.13.1` across `package.json`, `package-lock.json`, `README.md`, `src/`, `tests/`, `docs/` — only the two intentional historical CHANGELOG references remain (the `## [1.13.1]` heading itself and its own diff-link line). Grepped `1.14.0` back to confirm every intended surface picked it up (`package.json`, `package-lock.json` ×2, `README.md`, `docs/feature_roadmap.md`, `CHANGELOG.md` ×3, `docs/REPO_MAP.md`, and the test file). Added a new `tests/release-metadata.test.js` "documents the Hosted Password Management release surfaces with resolvable links" block (following the exact pattern of every prior per-feature entry in that file, including 044's — the most recent, itself written pre-merge) that asserts every new cross-link string is present and every new file path exists (`existsSync`), so this isn't just a one-time manual check — it's now a standing regression test. Confirmed the running app renders the new version: `tests/release-metadata.test.js`'s own assertion imports the real `APP_VERSION` (Vite-define-derived from `package.json`) and checks it resolves to `v1.14.0` — the same mechanism a real build uses.
- [x] T035 Full suite — `npm run test:run` and `npm run lint` clean at the Release Prep commit.
  - **Done (2026-07-10)**: `npm run test:run` → 146 files / 2012 tests pass. `npm run lint` → clean (its own banner now reads `project-alice@1.14.0`, incidental confirmation the version bump took effect end-to-end).

---

## Phase 07: Browser Smoke Test

**Purpose**: Verify the feature end-to-end in a real browser against a running hosted-mode deployment (or `npm run dev` pointed at a real Supabase test project — see quickstart.md Prerequisites). Required for any feature with user-facing UI.

**Setup**: hosted or hosted-equivalent local run with a real Supabase test project; a test account to sign in with; access to trigger/receive a real recovery email (or the Supabase dashboard's email log).

- [x] T036 [US-1] Change Password — complete spec.md's Independent Test in browser (correct current password → success; wrong current password → inline error, overlay stays open); verify sign-out/sign-in with the new password.
  - **Pass (2026-07-10, operator walkthrough against the Vercel preview / PR #138)**. Found in the same session, fixed post-walkthrough: the modal's `.pcf-input` password fields showed a duplicate reveal icon in Edge (the browser's native `::-ms-reveal` control overlapping the app's own eye toggle) — the v1.12.5 fix for this exact bug only covered `.auth-form__input`/`.conn-panel__field .edit-field__control`, not `PasswordChangeModal.js`'s separate `.pcf-*` class namespace. Extended the same `::-ms-reveal`/`::-ms-clear` suppression rule to `.pcf-input-wrap .pcf-input`; added regression coverage in `tests/components/welcome.test.js`.
- [x] T037 [US-2] Forgot Password request — complete the Independent Test; verify identical confirmation copy for a registered and an unregistered email.
  - **Pass (2026-07-10)**. Found and fixed: the `forgot_sent` "Check your inbox" confirmation had almost no gap between the confirmation text and "Back to sign in" (`.auth-overlay__center .auth-overlay__verification-text`'s `margin-bottom` was overridden to `4px`, on top of `.auth-overlay__back-link`'s own `4px` top margin — 8px total, visibly cramped). Bumped to `16px`. `recovery-expired` was unaffected (it doesn't use the `.auth-overlay__center` wrapper).
- [x] T038 [US-3] Reset Password via a real link — complete the Independent Test; verify the recovery session ends and the user lands on a logged-out Welcome/login, then successfully signs in with the new password. Also verify AC-11a: open a second real recovery link and, without submitting, close it via the × button — confirm the same "ends up on a logged-out Welcome/login" outcome (no success toast this time), and that returning to the app afterward does not silently land back in the Reset Password overlay.
  - **Blocked (2026-07-10)**: the first real recovery-link click landed on **Tracker directly**, not the Reset Password overlay — but the URL was a **production** Alice deployment, not the PR preview: `VITE_AUTH_EMAIL_REDIRECT_URL` is baked into the bundle at build time (`src/services/supabaseClient.js` reads it directly via `import.meta.env`, no runtime override), so whatever value is configured for the Preview environment determines where the email link actually lands, independent of which deployment sent it. Landing on production (which has none of this feature's code) is the expected symptom of that misdirect, not evidence of a bug in this PR. A second attempt was blocked by a `429` — identified as **Supabase's own abuse-protection rate limit on `resetPasswordForEmail`** (not a Vercel-side limit), which paces password-recovery email requests per address/project regardless of which frontend triggered them — before the redirect-URL issue could be corrected and retried. **Still unverified**: the actual Reset Password overlay behavior against a real link, and AC-11a's abandon-path. Needs a retry once Supabase's rate-limit window clears, with Preview's `VITE_AUTH_EMAIL_REDIRECT_URL` pointed at either `localhost:5173` (test via `npm run dev`) or this PR's actual preview host.
  - **Real root cause found and fixed (2026-07-11)**, via temporary diagnostic logging on a real deployed preview build (needed — extensive source-level analysis of `@supabase/auth-js` and unit tests against simulated event sequences both failed to surface this; only observing the actual event sequence in a real browser did): even with the redirect-URL corrected, a **valid** recovery link with a correctly-configured preview host still landed on Tracker. The console trace showed `PASSWORD_RECOVERY` firing and correctly resolving `password-recovery` first — then a **second** `INITIAL_SESSION` event arriving immediately after, carrying the same session. The original guard design fully "opened the gate" (`guardArmed = false`) the instant `PASSWORD_RECOVERY` resolved once, so this second event fell straight through to `applySession()` and silently overwrote `password-recovery` with `authenticated` — `main.js`'s `render()` briefly mounted the Reset Password overlay, then immediately swapped it for Tracker once the overwrite landed. This contradicts research.md D1's documented event-ordering assumption (based on a single registration's *expected* sequence, not accounting for a possible second/duplicate event arriving after the first has already resolved) — a genuine gap in the original design, not something the Phase 01 unit tests (which never simulated a second post-resolution event) could have caught.
    - **Fix**: `src/data/authStore.js`'s `init()` adds `recoveryGuardResolved` — set `true` by `resolvePasswordRecovery()` (and, per a follow-up finding below, also by `resolveRecoveryExpired()`), and while `true` the `onAuthStateChange` handler now holds *every* event except an explicit `SIGNED_OUT` (the real success-path `signOut()` after a password update, or the abandon-path `close()`-triggered one), instead of the old behavior of accepting anything once the initial guard disarmed. A `SIGNED_OUT` while `recoveryGuardResolved` correctly transitions to `unauthenticated` and clears the flag, so a later genuine sign-in still resolves normally — this isn't a permanent lockout, just event-filtering scoped to "while the recovery guard has already resolved."
    - **Tests added** (`tests/data/authStore.test.js`): a second `INITIAL_SESSION` after `PASSWORD_RECOVERY` no longer overwrites `password-recovery`; a second `SIGNED_IN` is held the same way (not just `INITIAL_SESSION` specifically); a genuine `SIGNED_OUT` after `PASSWORD_RECOVERY` still correctly transitions to `unauthenticated`; and a later real sign-in after that `SIGNED_OUT` still resolves normally (confirms the filter clears, not a permanent lockout).
    - **Diagnostic logging removed**: the temporary `[045-debug]` console tracing (commit `09e1db3`) that surfaced this has been removed now that the root cause is found and fixed.
    - **Pass, confirmed in-browser (2026-07-11)**: re-tested against the fixed build (commit `b68e710`) — Reset Password overlay opened correctly from a real link, submitted a new password successfully, confirmed logged out and able to sign in with the new password. Confirmed OK on all tested mobile/desktop viewports. AC-11a not separately re-confirmed this round (time-boxed by the SMTP rate limit) but the same guard fix covers its abandon path structurally.
- [~] T039 [US-4] Expired/invalid recovery link — open an already-used or manually-invalidated link; confirm the dedicated expired-link state appears immediately, with a working path back to Forgot Password.
  - **Blocked (2026-07-10)**: same Supabase-side `429` rate limit as T038 prevented triggering a second email to test against. Not yet verified in browser; unit/integration coverage exists (`tests/pages/welcome/ResetPasswordForm.test.js`, `tests/components/welcome.test.js`'s recovery-expired suite) but per this phase's own "Note" below, that's not a substitute for the real-browser pass this task calls for.
  - **Real bug found and fixed (2026-07-10)**, via Supabase's Admin API `generate_link` (bypasses the SMTP `429` entirely — no email send, so this unblocked live testing of the *failure* path even though a real end-to-end email is still pending): opening an intentionally-expired token landed on `#error=access_denied&error_code=otp_expired&error_description=Email+link+is+invalid+or+has+expired`, **not** the dedicated "This reset link has expired" state — the user was left on a plain Welcome page carrying the *wrong* banner: "Email verified. You can sign in now." (a signup-verification message, since nothing about a password reset had succeeded).
    - **Root cause**: both `authStore.js`'s recovery guard and `WelcomePage.js`'s verification-banner suppression keyed off a single marker, `RECOVERY_URL_MARKER` (`type=recovery`) — present on Supabase's *success*-path redirect, but **absent entirely** from its *failure*-path redirect for an expired/invalid/already-used token. Neither check could tell a failed recovery attempt apart from an ordinary page load, let alone from a failed signup-verification link (which shares the same `?auth=callback` redirect URL).
    - **Fix**: `src/data/authStore.js` adds `RECOVERY_FLOW_MARKER` (`flow=recovery`, exported alongside the existing `RECOVERY_URL_MARKER`) and `withRecoveryFlowMarker(url)`, a helper that appends it to a redirect URL. `src/pages/welcome/ForgotPasswordForm.js` now passes `withRecoveryFlowMarker(emailRedirectUrl)` as `resetPasswordForEmail`'s `redirectTo` (computed once at module load) instead of the raw shared `emailRedirectUrl` — Supabase preserves whatever base URL it's given as `redirect_to` on *both* success and failure, so this marker survives the failure path where `type=recovery` doesn't. `SignupForm.js`'s own redirect is deliberately left unmarked, so a failed signup-verification link can never be misclassified as an expired password-reset link — the "more specific" requirement raised during this fix. `authStore.js`'s guard now arms on *either* marker, and — new — `hasRecoveryErrorMarker()` detects an explicit Supabase failure (`error=`/`error_code=`) alongside the marker and resolves `recovery-expired` **immediately** rather than waiting out the full 8s guard timeout (avoiding a blank/`initializing` page for up to 8 seconds when Supabase has already told us the answer). `WelcomePage.js`'s banner-suppression check picks up the same new marker.
    - **Tests added**: `tests/data/authStore.test.js` — `withRecoveryFlowMarker` unit tests, a `flow=recovery`-only URL arming the guard, the immediate (non-timeout) `recovery-expired` resolution on an explicit error, an error with *neither* recovery marker not arming the guard at all (confirms non-misclassification), and the guard properly disarming afterward for a later real sign-in. `tests/components/welcome.test.js` — the exact real-world URL shape (`flow=recovery` + `error_code=otp_expired`, no `type=recovery`) no longer shows the wrong banner.
    - **Second, related bug found and fixed (2026-07-11)**, discovered while re-testing T039 for real against the T038 fix: reusing the same (now-consumed) real recovery link landed on the Welcome screen's **login** overlay — not the "This reset link has expired" state, and with no error toast. Root cause was the exact same class of bug as T038's fix: `resolveRecoveryExpired()` never set the new sticky-session flag (it only protected the `password-recovery` success path), so a later unguarded event resolving `applySession(null)` → `unauthenticated` tripped `main.js`'s Phase 04 "an ended recovery session returns to login" logic, silently bouncing the user off the expired-link screen before they could see it. Fixed by renaming the flag to `recoveryGuardResolved` and setting it in *both* `resolveRecoveryExpired()` and `resolvePasswordRecovery()` — `recovery-expired`'s own legitimate actions ("Request a new link", closing the overlay) never fire a `SIGNED_OUT` anyway, so this state simply never unlocks from that path once reached, which is correct. Added two more tests: a later `SIGNED_IN` after `recovery-expired` is now held (status stays `recovery-expired`), and a genuine `SIGNED_OUT` after `recovery-expired` still transitions to `unauthenticated` with a later real sign-in resolving normally afterward.
    - **Still outstanding**: this second fix (commit after `b68e710`) is unit-tested but not yet re-verified in-browser — needs a retry once the Supabase rate-limit window clears, since T039's real-link test is what surfaced it in the first place.
- [x] T040 [US-5] Local Mode isolation — in a Local Mode run, confirm no Welcome page, no Forgot Password link, no Change Password control anywhere.
  - **Pass (2026-07-10)**. Unrelated finding during setup: `npm run dev` initially showed a fully blank page with a browser ad-blocker blocking `@vercel/analytics`'s dev-server chunk (`ERR_BLOCKED_BY_CLIENT`) — resolved by disabling the blocker. Confirmed pre-existing (from the v1.13.0 Web Analytics feature, not touched by 045) and out of this PR's scope per [[feedback_scoped_tickets]]; flagged to the user as a candidate for its own issue, not fixed here.
- [x] T041 [US-6] Demo Mode isolation — enter Demo Mode; confirm no active Change Password control in Settings.
  - **Pass (2026-07-10)**.
- [x] T042 Mobile layout — DevTools ≤ 640px for all three new overlays (`PasswordChangeModal`, `ForgotPasswordForm`, `ResetPasswordForm`, and the `recovery-expired` view); confirm no clipping/overflow and all interactions work with touch/click.
  - **Pass (2026-07-10)**, with two Galaxy Z Fold 5-specific observations explicitly flagged by the user as non-actionable ("no actionable items," "okay for other mobile viewports"): the Change Password modal's "Update password" button label wraps to two lines, and the Forgot Password overlay's title/subtitle wrap to two lines. Left as-is at the user's own call — normal at every other tested mobile width, and the Z Fold 5's ~280–344px folded width is an extreme outlier this feature's scoped-down fidelity gate doesn't specifically target.

**Note**: each task is complete only when a human (or a vision-capable agent, per plan.md's Tier 2 judge) has walked through the steps against the to-be-merged state and all pass criteria are met. Since Tier 1 was skipped for this feature (plan.md Visual-Fidelity Mode), this smoke test is the first end-to-end look at the built overlays against real browser rendering — expect it to possibly surface layout issues Tier 2's static screenshots didn't catch, and treat that as normal for this feature's scoped-down fidelity gate, not a process failure.

**Outstanding before this phase can close**: T038/T039 need a retry once the `429` rate limit clears (and, for T038, a redirect-URL fix on the testing side — not a code fix). T018/T019's Tier 2 visual review (carried over from Phase 04) is still open.
